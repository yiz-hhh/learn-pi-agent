{
  "name": "fixture-calculator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "12 章 E2E 临时项目：含故意 bug 的计算器（供 Coding Agent 修复与验证）",
  "devDependencies": {
    "tsx": "*"
  },
  "scripts": {
    "test": "node --import tsx tests/calculator.test.ts",
    "start": "node --import tsx src/index.ts"
  }
}
